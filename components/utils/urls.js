const BASE_URL = "https://developeruche.herokuapp.com"

const OTHERS = {
    portfolio: `${BASE_URL}/api/projects`,
    testimonial: `${BASE_URL}/api/testimonial`,
    messageMe: `${BASE_URL}/api/messageme/`,
}


let All = {
    BASE_URL,
    OTHERS
}

export default All